[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["apps/api/**", "packages/core/**", "Dockerfile"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
