{
  "name": "econav",
  "version": "1.0.0",
  "private": true,
  "description": "CityConnect smart city platform — citizen services and municipal operations (EcoNav waste routing)",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "bash scripts/dev-local.sh",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:migrate": "npm run migrate --workspace=@econav/db",
    "db:seed": "npm run seed --workspace=@econav/db",
    "db:prepare": "npm run build --workspace=@econav/db && npm run db:seed",
    "dev:worker": "npm run worker:notifications --workspace=@econav/api",
    "dev:api": "npm run dev --workspace=@econav/api",
    "dev:web": "npm run dev --workspace=@econav/web",
    "dev:mobile": "npm run start --workspace=@econav/mobile",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "setup:deploy": "bash scripts/setup-deploy.sh",
    "deploy": "bash scripts/deploy.sh",
    "go-live": "bash scripts/go-live.sh"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@railway/cli": "^5.52.1",
    "vercel": "^41.0.0"
  }
}
